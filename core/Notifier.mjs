import {percentBetweenValues} from './utils.mjs'

class Notifier {

	/**
	 *
	 * @param {Object} tick
	 * @param {Array} coins
	 */
	constructor(tick, coins) {
		this.tick = tick
		this.coins = coins

		this.run()
	}

	run() {
		console.log('ok')

		const object = {}

	}
}

export default Notifier